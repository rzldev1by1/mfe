export const closeModal = ({modal, setModal}) => {
    const newModal = {...modal}
    newPage.modal = false
    setPage(newModal)
}
export const showModal = ({modal, setModal}) => {
    const newModal = {...modal}
    newPage.modal = false
    setPage(newModal)
}